const todos = (state = [], action) => {
    switch (action.type) {
        case 'ADD_TODO':
            
        case 'TOGGLE_TODO':
            
        default:
            
    }
}
export default todos